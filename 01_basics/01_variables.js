const accountId = 123456
let accountName = "ABCD"
let accountEmail = "Abcd@google.com"
var accountPassword = "1233455678"

let accountState;
//if we use keyword and do not assign to it then it will show undefined 

/* var is not used nowdays in javascript beacuse due to scope means { }
prefer not use var
because of issue in block scope and functional scope
*/


accountCity = "Jaipur"

console.log(accountId);

console.table([accountId, accountName, accountEmail, accountPassword, accountCity,accountState])